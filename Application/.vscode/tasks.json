{
	"version": "2.0.0",
	"tasks": [
        {
            "label": "Make Flash",
            "type": "shell",
            "command": "make clean && make && scp ./Build/Blink.bin pc:/Network_File_System/RFS/home/root/Binary.bin",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
	]
}